(require 'highlight-regexp)
(setq highlight-regexp-recenter-clears-p nil)
(setq highlight-regexp-faces '(secondary-selection
                               show-paren-match-face ;; default
                               highlight 
                               trailing-whitespace))

